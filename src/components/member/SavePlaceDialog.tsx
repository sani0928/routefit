"use client";
import { X } from "lucide-react";
import type { MemberPlaceList } from "@/features/member/types";
import type { Place } from "@/features/route-optimization/types/route.types";
export function SavePlaceDialog({ place, lists, onSave, onClose }: { place: Omit<Place, "id" | "type"> | null; lists: MemberPlaceList[]; onSave: (listId: string) => void; onClose: () => void }) { if (!place) return null; return <div className="save-place-backdrop" role="presentation" onMouseDown={onClose}><section className="save-place-dialog" role="dialog" aria-modal="true" aria-label="장소 저장" onMouseDown={(event) => event.stopPropagation()}><header><div><div><p>장소 리스트에 저장</p><strong>{place.name}</strong></div></div><button type="button" onClick={onClose} aria-label="닫기"><X size={18} /></button></header><p>{place.address || `${place.latitude.toFixed(5)}, ${place.longitude.toFixed(5)}`}</p><div className="save-list-options">{lists.map((list) => <button type="button" key={list.id} onClick={() => onSave(list.id)}><span style={{ backgroundColor: list.color }} />{list.name}</button>)}</div>{lists.length === 0 && <small>먼저 장소 리스트를 만들어 주세요.</small>}</section></div>; }