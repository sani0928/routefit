import { NextResponse } from "next/server";
import { z } from "zod";
import { getSessionUser, unauthenticated, badRequest } from "@/lib/member/api";
import { deletePlaceList, getPlaceLists, getSavedPlaces, updatePlaceList } from "@/lib/member/repository";
import { LIST_COLORS } from "@/features/member/types";
const bodySchema = z.object({ name: z.string().trim().min(1).max(60).optional(), color: z.enum(LIST_COLORS).optional() });
export async function GET(_: Request, context: { params: Promise<{ listId: string }> }) { const user = await getSessionUser(); if (!user) return unauthenticated(); const { listId } = await context.params; const places = await getSavedPlaces(user.id, listId); return places ? NextResponse.json({ places }) : NextResponse.json({ error: { message: "리스트를 찾을 수 없습니다." } }, { status: 404 }); }
export async function PATCH(request: Request, context: { params: Promise<{ listId: string }> }) { const user = await getSessionUser(); if (!user) return unauthenticated(); const body = bodySchema.safeParse(await request.json()); if (!body.success) return badRequest("수정할 리스트 정보가 올바르지 않습니다."); const { listId } = await context.params; const item = await updatePlaceList(user.id, listId, body.data); return item ? NextResponse.json({ lists: await getPlaceLists(user.id) }) : NextResponse.json({ error: { message: "리스트를 찾을 수 없습니다." } }, { status: 404 }); }
export async function DELETE(_: Request, context: { params: Promise<{ listId: string }> }) { const user = await getSessionUser(); if (!user) return unauthenticated(); const { listId } = await context.params; return (await deletePlaceList(user.id, listId)) ? NextResponse.json({ lists: await getPlaceLists(user.id) }) : NextResponse.json({ error: { message: "리스트를 찾을 수 없습니다." } }, { status: 404 }); }